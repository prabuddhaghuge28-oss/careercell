// Notification model removed during revert. This stub prevents require() errors
// and indicates that the project should use the existing Notice model/collection.
console.warn('notification.model.js: notifications collection removed; using notices instead.');
module.exports = null;
